{
  "name": "prompt-eiken-h3",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "python scripts/generate-index.py && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/catalog.test.js"
  },
  "dependencies": {
    "@fontsource/dm-sans": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "dompurify": "^3.2.0",
    "lucide": "^0.468.0",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "vite": "^6.1.0"
  }
}
